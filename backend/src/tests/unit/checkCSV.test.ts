/* Tests for the checkCSV middleware */
// import checkCSV from "../../middleware/checkCSV";
import { describe, test, expect } from "@jest/globals";

describe("CheckCSV middleware", () => {
    test('Should call next function if CSV file is included', () => {
        
    })
})